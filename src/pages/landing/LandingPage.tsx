import React from 'react';
import { LoginPage } from '../auth/LoginPage';

/**
 * Bethel St. Paul Hospital - Transformed Landing & Staff Login Page.
 * The public landing page has been unified into the staff login and welcome workspace
 * while preserving all original branding, telemetry badges, animations, and visual hero aesthetics.
 */
export const LandingPage: React.FC = () => {
  return <LoginPage />;
};

export default LandingPage;
